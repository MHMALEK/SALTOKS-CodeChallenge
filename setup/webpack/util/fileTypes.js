/**
 * file types for webpack
 */
module.exports = {
  // js files
  jsAndJsx: /\.(js|jsx)$/,
  // css related files
  styles: /\.s?css$/,
  // font files
  font: /\.(eot|ttf|woff|woff2|svg)$/,
  // image related files
  images: /\.(jpg|png|jpe?g|gif)$/,
};
